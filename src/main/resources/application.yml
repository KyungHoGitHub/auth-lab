# init project
spring:
  application:
    name: auth-service

  data:
    redis:
      host: localhost
      port: 6380
  cache:
    type: redis
  # database setting
  datasource:
    url: jdbc:postgresql://localhost:5432/authserver_db
    driver-class-name: org.postgresql.Driver
    username: ykh12
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

#
#  kafka:
#    bootstrap-servers: localhost:9092
#    producer:
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
#    consumer:
#      group-id: auth-service-group
#      auto-offset-reset: earliest
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#      properties:
#        spring.json.trusted.packages: com.example.auth-service

google:
  client-id: ${GOOGLE_CLIENT_ID:780923997453-3ftob69gfdffi9qinnm7rcj5qmfjqk74.apps.googleusercontent.com}

# application.yml
jwt:
  secret: 78B354F83E36BBF47958A658899A978B354F83E36BBF47958A658899A9
  expiration: 3600000
  refresh-expiration: 7200000
  issuer: test-user

server:
  port: 8081

logging:
  level:
    root: info   # 전체 기본 레벨
    com.example: debug # 특정 패키지만 debug
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"
